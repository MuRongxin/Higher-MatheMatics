### 若叶睦发电文学——求极限

## 函数求极限
### 无穷小量
**核心思想**：无限趋近于零，但又不是零。

开始“无穷小”的想象：

    现在，想象这只蚂蚁开始缩小。

    它缩小到只有原来的一半大小（0.5厘米）... 再缩小一半（0.25厘米）... 再缩小一半（0.125厘米）...

    你不停地让它缩小下去，永无止境。

    这个不断缩小的过程中产生的每一个“蚂蚁”，相对于足球场来说都越来越微不足道，但它们本身都大于零（还没缩到真正消失）。

    这个“不断缩小的、越来越微不足道的、但始终大于零的量”，在数学上我们就叫它“无穷小量”。

关键点解释：

- “无限趋近于零”： 就像那只不断缩小的蚂蚁，无穷小量可以要多小有多小，比你能想到的任何正数（比如0.0000001，或者更小）都还要小。你想让它多接近零，它就能多接近零。

- “但不是零”： 它永远不会真正等于零。它就是一个无限接近于零的、动态的、极小的量。如果它等于零了，很多微积分的计算就失去意义了。

-  一个“过程”，一个“趋势”： 无穷小量更像是在描述一个不断变小、无限接近零的状态或过程，而不是一个固定的、具体的数字。就像你永远无法说“那只蚂蚁缩小到了无穷小量那么大”，因为它还在继续缩小。

- 相对性： “小”是相对的。对于地球来说，一只蚂蚁是微小的；对于太阳系来说，地球本身也可以看作是一个“无穷小量”。在微积分里，我们通常是在某个特定的变化过程（比如自变量x趋近于某个值a）中，讨论函数的变化量或者差值是不是无穷小量。

- 数学上的记号： 常用希腊字母 α (alpha) 或 β (beta) 来表示一个无穷小量。我们常说：如果当 x -> a (x趋近于a) 时，函数 f(x) - L -> 0 (趋近于0)，那么 f(x) - L 就是一个当 x -> a 时的无穷小量。这里 L 通常是 f(x) 在 x=a 处的极限值。

如果函数 f(x)满足 $\lim_{⁡x→a} f(x)=0$，则称 $f(x)$是 $x\to a$ 时的无穷小量。

> 已知条件：$lim_{⁡x→1}(x−1)^2=0$（因为当 x 趋近于 1 时，(x−1) 趋近于 0，其平方也趋近于 0）。
>>根据定义，$\lim_{⁡x→1}(x−1)^2=0$ 成立，因此 $(x−1)^2$ 是 $x→1$ 时的无穷小量。


**为什么需要“无穷小量”？（微积分的核心）**

微积分（尤其是微分）的精髓就在于研究变化。无穷小量是描述瞬时变化率的关键工具：

**无穷小量的分类**

依据主要是它们趋近于0的相对速度。在微积分中，当自变量 x 趋近于某个点（通常是 (0)）时，不同的无穷小量趋近于0的速度可能不同。为了精确比较这种速度，数学家引入了**“阶”**的概念。以下是无穷小量分类的核心依据和具体分类方法：
设 α(x) 和 β(x) 都是当 x→a 时的无穷小量（即$\lim_{x \to a} α(x)=0，\lim_{x \to a} \beta(x) = 0$）。我们通过计算极限 $\lim_{x \to a} \frac{\alpha(x)}{\beta(x)}$ 来比较它们的趋近速度，并据此分类

> 如何更好的理解趋近于0的相对速度：

- 不能简单的像下面这样计算理解：

| 时间段 | Δt | A (x) Δ位置 | B (x²) Δ位置 | A 速度 | B 速度 | 速度比 vA/vB |
|--------|----|-------------|--------------|--------|--------|--------------|
| 1→0.5  | 0.5 | 1→0.5 = -0.5 | 1→0.25 = -0.75 | 0.5/0.5 = 1 | 0.75/0.5 = 1.5 | 1/1.5 = 2/3 |
| 0.5→0.1 | 0.4 | 0.5→0.1 = -0.4 | 0.25→0.01 = -0.24 | 0.4/0.4 = 1 | 0.24/0.4 = 0.6 | 1/0.6 = 5/3 |
| 0.1→0.01 | 0.09 | 0.1→0.01 = -0.09 | 0.01→0.0001 = -0.0099 | 0.09/0.09 = 1 | 0.0099/0.09 ≈ 0.11 | ≈9.09 |
| 0.01→0.001 | 0.009 | 0.01→0.001 = -0.009 | 0.0001→0.000001 = -0.000099 | 0.009/0.009 = 1 | 0.000099/0.009 ≈ 0.011 | ≈90.9 |
| 0.001→0.000001 | 0.000999 | 0.001→0.000001 = -0.000999 | 0.000001→0.000000001 = -0.000000999 | 1 | ≈0.001 | ≈1000 |

这里的关键在于我们如何定义“趋近于0的速度”。我们衡量的不是在某个时间点离起点有多远，而是在接近终点（0）的过程中，谁“消失”得更快。，“趋近于0的速度快”，指的是“离0的距离缩小的快”

从你计算的“速度”来看，f(x)=x² 的“速度”确实在变慢，而 g(x)=x 的“速度”是恒定的。 
那么，这和我们说的“x² 是 x 的高阶无穷小，趋近于0的速度更快”是不是矛盾了呢？ 
不矛盾。 这恰恰触及了问题的核心，也是最容易混淆的地方。我们混淆了两种完全不同的“速度”。 

**“瞬时速度”只描述了“那一瞬间”的情况，而忽略了“当前离终点还有多远”。**

速度一：函数值 y 相对于自变量 x 的变化率（你计算的速度） 

你计算的是 Δy / Δx，这其实就是函数导数的近似。 

    对于 g(x) = x，它的导数是 g'(x) = 1。这表示，无论 x 在哪里，x 每减小一个单位，g(x) 的值也精确地减小一个单位。它的“瞬时速度”恒定为1。
     
    对于 f(x) = x²，它的导数是 f'(x) = 2x。这表示，f(x) 的“瞬时速度”是 2x。当 x 越来越接近0时，2x 也越来越接近0。所以，f(x) 的值减慢了。
结论：从“y值随x变化的快慢”这个角度看，你的观察完全正确。x² 的变化速度确实在变慢。 

速度二：函数值 y 本身趋近于极限 0 的速度（高阶无穷小的“速度”）  

    我们在意的不是“谁在y轴上跑得快”，而是 “谁更快地抵达终点” 。 
  
    这怎么衡量呢？我们不看它们的绝对速度，而是看它们的相对位置。      



我们计算它们的位置比：
$\frac{B的位置}{A的位置}​=\frac{f(x)}{g(x)}=\frac{x^2}{x}$；现在，我们再来看这个“位置比”在 x → 0 的过程中的变化：

| 时间点 (x) | A的位置 (g(x)=x) | B的位置 (f(x)=x²) | **位置比 f(x)/g(x)** | A的瞬时速度 (g'(x)) | B的瞬时速度 (f'(x)) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| x = 1 | 1 | 1 | 1 | 1 | 2 |
| x = 0.5 | 0.5 | 0.25 | **0.5** | 1 | 1 |
| x = 0.1 | 0.1 | 0.01 | **0.1** | 1 | 0.2 |
| x = 0.01 | 0.01 | 0.0001 | **0.01** | 1 | 0.02 |
| x = 0.001 | 0.001 | 0.000001 | **0.001** | 1 | 0.002 |

请看“位置比”这一列！ 

当 x 趋近于0时，f(x)/g(x) 的值也趋近于0。这意味着，B的位置相对于A的位置，已经微不足道了。可以说，粒子B已经“到达”了终点，而粒子A还在路上。 

这就是“高阶无穷小”的真正含义：它描述的是一种“相对位置”的坍缩速度，而不是“瞬时速度”的快慢。

一个更贴切的比喻：刹车 

想象两辆车都以100km/h的速度行驶，目标是在0点处的一堵墙前停下。 

     车A (g(x)=x)：它以恒定的减速度刹车，直到撞墙前瞬间速度才变为0。它的速度变化曲线是线性的。
     车B (f(x)=x²)：它一开始就猛踩刹车，速度迅速降到一个非常低的值，然后“蠕动”着靠近墙壁。
     

现在问：哪辆车 更快地完成了“从100km/h到静止” 这个过程？显然是车B。虽然车B在最后阶段的“蠕动”速度比车A在某个中间点的速度要慢，但它更早地进入了“几乎静止”的状态。 

x² 就是这辆“猛踩刹车”的车。它在 x 从1变到0.1的区间里，其值从1锐减到0.01，已经完成了99.9%的“趋近于0”的旅程。而 x 在同样的区间里，才从1减到0.1，只完成了90%的旅程。 

高阶无穷小的“速度”：是 lim(f(x)/g(x))，即两个函数相对位置的趋近速度。在这个意义上，x² 的位置相对于 x 的位置更快地坍缩到了0。

举个生活中的例子：
一辆法拉利（g(x)）在距离终点1公里的地方，以100km/h的速度飞驰。
一辆蜗牛（f(x)）已经爬到了距离终点线1厘米的地方，速度是0.001km/h。 

请问：谁会更快地抵达终点？
显然是蜗牛！虽然它的瞬时速度极慢，但它离终点已经近到可以忽略不计了。 

无穷小的阶数关注的是函数值本身趋近 0 的速度，而不是变化率。

直接比较 f'(x) 和 g'(x)，就像只看法拉利和蜗牛的仪表盘，却不看它们各自的位置，这显然是片面的。 

f(x) 和 g(x) 都在向0移动。我们想知道，在移动的过程中，f(x) 相对于 g(x) 来说，是不是
**“更快地完成了它的旅程”**。

$R(x) = \frac{f(x)}{g(x)} = \frac{x^2}{x} = x$

这个 R(x) 就是“相对位置”。它回答了这样一个问题：“在 x 这个时刻，f(x) 离终点的距离，是 g(x) 离终点距离的几分之几？” 

我们来看 R(x) 的变化： 

     当 x=0.1 时，R(0.1) = 0.1。这意味着 f(x) 的路程只剩下 g(x) 的10%了。
     当 x=0.01 时，R(0.01) = 0.01。这意味着 f(x) 的路程只剩下 g(x) 的1%了。
     当 x=0.001 时，R(0.001) = 0.001。这意味着 f(x) 的路程只剩下 g(x) 的0.1%了。
     

看到了吗？随着比赛的进行，f(x) 相对于 g(x) 来说，它“剩下的任务量”在以极快的速度缩减。 
当 x → 0 时，R(x) → 0。这个极限为0的最终结果，就宣告了：f(x) 已经率先“抵达终点”，它的任务相对于 g(x) 来说已经完成了。 

所以，“相对位置”的极限，是衡量“谁更快完成旅程”的完美指标。 

数学上的终极连接：洛必达法则 (L'Hôpital's Rule)

1. 高阶无穷小
    > 定义：若 $\lim_{x→a}\frac{α(x)}{β(x)} =0$，则称 α(x) 是比 β(x) 高阶的无穷  小。
    > 含义：α(x) 趋近于0的速度比 β(x) 快得多。
    > 
    > 记号：\(\alpha(x) = o(\beta(x))\)（当 \(x \to a\)）。
     
- 例子：
当 x→0 时，$x^2$是比 $x$ 高阶的无穷小，因为：$$\lim_{x\to 0}\frac{x^2}{x} = 0$$
当 $x→0$时：$x^2$ 的值比 $x$ 的值更小（趋近 0 更快）, 因此，尽管在$x\to 0$时， $x$ 的变化率更大，但 $x^2$ 的函数值更接近 0，所以它是高阶无穷小。


2. 同阶无穷小
    > 定义：若 \(\lim_{x \to a} \frac{\alpha(x)}{\beta(x)} = c \neq 0\)，则称 α(x) 与 β(x) 是同阶无穷小。
    > 含义：α(x) 和 β(x) 趋近于0的速度成比例，同步。
- 例子：
当 $x→0$ 时，$2x$ 和 $x$ 是同阶无穷小，因为：$$\lim_{x\to 0}\frac{2x}{x} = 2$$

3. 等价无穷小
    > 定义：若 \(\lim_{x \to a} \frac{\alpha(x)}{\beta(x)} = 1\)，则称 α(x) 与 β(x) 是等价无穷小。
    > 含义：α(x) 和 β(x) 趋近于 0 的速度相同。
    > 记号：\(\alpha(x) \sim \beta(x)\)（当 \(x \to a\)）
- 例子：
当 $x→0$ 时，$sinx$ 和 $x$ 是等价无穷小量，因为：$$\lim_{x\to 0}\frac{sinx}{x} =1$$
$tanx ∼x,arcsinx ∼x,\ln(1+x) ∼x,e^x−1∼x$.

4. 低阶无穷小
    > 定义：若 \(\lim_{x \to a} \frac{\alpha(x)}{\beta(x)} = \infty \)，则称 α(x) 是比 β(x) 低阶的无穷小。或 $\beta(x) / \alpha(x) \rightarrow 0$。
    > 含义：α(x) 趋近于 0 的速度比 β(x) 慢。
    > 记号：\(\alpha(x) = o(\beta(x))\)（当 \(x \to a\)，且\(\alpha(x) \neq 0\))。
    > 注意，如果两个无穷小量的阶数是确定的，它们就可能是等价的，或者 $\alpha(x)\sim β(x)$。但反过来，当两个无穷小量是等价的时，它们的阶数就不一定可以确定了（即可能等价的无穷小不一定是同阶的）。
    > 但等价的无穷小一定是同阶的无穷小。
- 例子：
当 $x→0$ 时，$x$是比 $x^2$ 低阶的无穷小。因为：$$\lim_{x\to 0}\frac{x}{x^2}=\lim_{x\to 0}\frac{1}{x} = \infty$$

5. k 阶无穷小
    > 定义：若存在常数 k>0 使得 \(\lim_{x \to a} \frac{\alpha(x)}{|\beta(x)|^k} = c\)，那么 α(x) 是关于$\beta(x)$的 k 阶无穷小，或 $α(x)= o(x^k),x\rightarrow a$。
    > 含义：α(x) 趋近于0的速度与 β(x) 的 k 次方相当
    等价无穷小是0阶，同阶无穷小是1阶，高阶无穷小是2阶，k阶无穷小是k阶

- 例子
  当 x→0 时，1−cosx 是关于 x 的 2 阶无穷小，因为：$$\lim_{x\to 0}\frac{1-cosx}{x^2} =\frac{1}{2}$$ 
  

一、核心思想：为什么可以替换？ 

想象一下，当 x → 0 时，sin(x) 和 x 都在奔向 0。虽然它们奔向 0 的“路径”不同，但它们的“速度”和“姿态”在终点附近几乎一模一样。 

等价无穷小的本质是：在 x → 0 的过程中，两个无穷小量的“差异”是更高阶的，可以忽略不计。 

数学上，f(x) 和 g(x) 是等价无穷小，意味着：
 limx→0​g(x)f(x)​=1 
 例如，lim(sin(x)/x) = 1，所以我们说 sin(x) ~ x。 

在求一个复杂的极限时，如果其中某个因子是 sin(x)，我们就可以大胆地把它换成 x，因为它们的“比值”在极限状态下是 1，替换后不会改变整个极限的值。这就像在百米冲刺的最后 1 米，用一个身高 1.80m 的人去替换另一个身高 1.79m 的人，对比赛结果毫无影响。 
二、黄金法则：何时才能使用？ 

这是最重要的一点，用错了就会满盘皆输！ 

法则：等价无穷小替换只能在乘、除运算中使用，不能在加、减运算中直接使用！ 

     正确用法：lim [f(x) * g(x)] 或 lim [f(x) / g(x)]
         你可以把 f(x) 或 g(x) 换成它们的等价无穷小。
         
     错误用法：lim [f(x) + g(x)] 或 lim [f(x) - g(x)]
         绝对不能直接把 f(x) 或 g(x) 换掉！这是最常见的错误，我们后面会举例说明。
         
     

三、常用等价无穷小替换表（当 x → 0 时） 

这张表是你必须牢记于心的“武功秘籍”。 
原式
 	
等价于
 	
记忆口诀
 
 sin(x)	x	三角函数基本款 
tan(x)	x	三角函数基本款 
arcsin(x)	x	反三角函数也基本 
arctan(x)	x	反三角函数也基本 
1 - cos(x)	x² / 2	特殊！ 这个是二阶的，要记牢 
ln(1 + x)	x	对数函数，1+x 变 x 
eˣ - 1	x	指数函数，减1后变 x 
aˣ - 1	x * ln(a)	指数函数变体，底a要提ln 
(1 + x)ᵅ - 1	α * x	万能公式！ α是任意常数 
 
  

推广：如果 u(x) → 0，那么上面表中的 x 都可以换成 u(x)。
例如：sin(3x²) ~ 3x²，ln(1 - x) ~ -x。 
四、实战演练：三步搞定 

步骤： 

     判断：看极限过程是不是 x → 0（或 u(x) → 0），以及无穷小是不是在乘除位置。
     替换：找到可以替换的部分，用上表中的等价无穷小进行替换。
     求解：计算替换后的、更简单的极限。
     

示例 1：基础应用 

求极限：lim(x→0) [sin(5x) / tan(2x)] 

     判断：x → 0，sin(5x) 和 tan(2x) 在分子分母，是除法。可以替换！
     替换：
         sin(5x) 等价于 5x
         tan(2x) 等价于 2x
         原式 = lim(x→0) [5x / 2x]
         
     求解：
         = 5 / 2
         
     

示例 2：组合应用 

求极限：lim(x→0) [ (eˣ - 1) * ln(1 + 3x²) ] / x³ 

     判断：x → 0，分子是两个因子相乘，分母是 x³。可以替换！
     替换：
         eˣ - 1 等价于 x
         ln(1 + 3x²) 等价于 3x²
         原式 = lim(x→0) [ (x) * (3x²) ] / x³
         
     求解：
         = lim(x→0) [3x³ / x³]
         = 3
         
     

五、避坑指南：加减法为什么不能直接替换？ 

这是初学者最容易掉进去的陷阱，我们来看一个经典的例子。 

求极限：lim(x→0) [tan(x) - sin(x)] / x³ 
错误解法 ❌ 

     判断：x → 0。
     错误替换：直接在加减法中替换！
         tan(x) 替换为 x
         sin(x) 替换为 x
         原式 = lim(x→0) [x - x] / x³ = lim(x→0) [0 / x³] = 0
         
     结果：错误！
     

为什么错？ 因为 tan(x) 和 sin(x) 虽然都等价于 x但它们与 x 的“差”是不同阶的无穷小。 

     tan(x) - x 是 x³/3 阶的。
     sin(x) - x 是 -x³/6 阶的。
    直接替换，相当于把这两个关键的“差异”项都给丢掉了，所以结果必然错误。
     

正确解法 ✅ 

当遇到加减法时，我们有两种策略： 

策略1：通分、因式分解，创造出乘除法 

     变形：
         tan(x) - sin(x) = sin(x)/cos(x) - sin(x) = sin(x) * (1/cos(x) - 1)
         = sin(x) * [(1 - cos(x)) / cos(x)]
         
     替换：现在整个表达式变成了乘除法！
         sin(x) ~ x
         1 - cos(x) ~ x²/2
         cos(x) → 1
         原式 = lim(x→0) [ x * (x²/2) / 1 ] / x³
         
     求解：
         = lim(x→0) [x³/2] / x³
         = 1/2
         
     

策略2：泰勒展开（更高级，但万能） 

如果遇到无法因式分解的情况，泰勒展开是“终极武器”。 

     sin(x) = x - x³/6 + o(x³)
     tan(x) = x + x³/3 + o(x³)
     tan(x) - sin(x) = (x + x³/3) - (x - x³/6) + ... = x³/2 + ...
     原式 = lim(x→0) [x³/2 + ...] / x³ = 1/2
     

总结 

     核心：记住替换表，理解其本质是“忽略高阶差异”。
     法则：只能在乘除法中用，加减法中绝对不能直接用！
     技巧：遇到加减法，先尝试通分、因式分解，把它变成乘除法。
     后手：如果实在不行，就用泰勒展开，它能解决一切（在课程允许范围内）。
     